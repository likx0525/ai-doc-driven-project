{
  "schema_version": 1,
  "status": "stub",
  "notes": "JSON-compatible YAML. Fill modules before treating this file as an architecture fact source.",
  "modules": [],
  "rules": {
    "dependency_direction": [],
    "forbidden_dependencies": [],
    "shared_modules": []
  },
  "checks": {
    "unknown_source_files": "warn",
    "enforce_allowed_dependencies": true,
    "scan_imports_for": ["python", "javascript", "typescript"]
  },
  "example_module": {
    "name": "domain",
    "description": "Example core domain layer; copy into modules and adjust for the actual project.",
    "paths": ["src/domain/**"],
    "import_roots": ["domain", "@/domain"],
    "allowed_dependencies": ["shared"],
    "forbidden_dependencies": ["infrastructure"],
    "owners": ["team-or-person"]
  }
}
