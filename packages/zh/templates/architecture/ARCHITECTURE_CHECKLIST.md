# ARCHITECTURE_CHECKLIST

> 文档状态：current

## 修改前检查

1. 是否影响模块边界。
2. 是否影响数据模型。
3. 是否影响业务规则。
4. 是否影响状态流。
5. 是否影响依赖方向。
6. 是否新增核心对象。
7. 是否新增核心状态。
8. 是否新增派生数据。
9. 是否把业务逻辑写进 UI 层。
10. 是否引入重复业务规则。
11. 是否引入重复状态。
12. 是否新增特殊判断。
13. 是否需要新增测试。
14. 是否需要更新架构文档。
15. 是否需要用户确认。

## 架构影响判断输出模板

### 架构影响判断

- 涉及层级：
- 涉及模块：
- 是否影响数据模型：
- 是否影响业务规则：
- 是否影响状态流：
- 是否改变依赖方向：
- 是否可能造成补丁化：
- 是否需要更新架构文档：
- 是否需要用户确认：

## 必须暂停并请求确认的情况

- 修改核心数据模型。
- 修改业务规则。
- 修改核心计算规则。
- 修改历史数据解释方式。
- 改变模块边界。
- 改变依赖方向。
- 新增全局状态。
- 跨多个模块大范围修改。
- 发现现有架构无法自然支持需求。
- 需要大范围重构。
