# VERSIONING

> 文档状态：draft

## 版本规则

- 待确认。

## 发布节奏

- 待确认。

## 版本号含义

- 待确认。

## 兼容性规则

- 待确认。

## 废弃策略

- 待确认。

## 部署约束

- 没有 `DEPLOYMENT.md`，不应进行正式部署。
- 没有 `ROLLBACK.md`，不应进行高风险发布。
- 涉及生产环境必须让人确认。
- 涉及数据迁移必须说明是否可回滚。
- AI 不能声称“已部署成功”，除非实际执行并记录证据。
- 部署结果必须记录到 `test-runs.md` 或 deploy 文档中。
